
# Timesheet Management System

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.

## Deployment

For detailed cPanel deployment instructions, please see [CPANEL_DEPLOYMENT.md](./CPANEL_DEPLOYMENT.md).
